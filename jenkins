version: '3.7'
services:
  jenkins:
    image: jenkins:jenkins:lts
    ports:
      - 2602:8080
      - 3602:5000
    volumes:
      - jenkins_data:/var/jenkins_home
    extra_hosts:
      - "swarm-manager:192.168.0.111"
    networks:
    - dockerNet

volumes:
  jenkins_data:

networks:
  dockerNet:
    external: true
